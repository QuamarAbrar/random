```python
import os

# Define the content for GEMINI.md
md_content = """# Project Antigravity: "Will You Forgive Me?" Interactive Experience
## High-Fidelity Design Document & Prompt-Engineering System

Project Antigravity is a high-end, high-fidelity interactive web experience designed as a deeply personalized gesture. The UI combines cutting-edge web design languages (Glassmorphism, Bento Grid, Floating Fluid Physics) with playful, interactive micro-interactions (an elusive, "runaway" negative response button) to deliver an unforgettable, emotionally resonant user journey.

---

## 1. Architectural UI/UX Framework

To elevate this from a standard webpage to a "costly," premium digital experience, the frontend incorporates three core structural paradigms:

### A. The Cinematic Intro (Screen 1)
* **Visual Style:** Deep, dark immersive background with a glowing neon/aurora fluid gradient mesh that moves dynamically based on gyroscope or mouse position.
* **Layout:** Centered minimalist typography utilizing an elegant variable serif font (`Hi Nishi, I Love You SM`).
* **Asset Frame:** A high-resolution portrait framed inside a smooth, continuous-curvature masking layout (squircle) with an ultra-thin glowing border accent.
* **Call to Action:** A high-fidelity, magnetic "Continue" button featuring a multi-layered drop shadow, backdrop blur, and an organic liquid hover animation.

### B. The Bento Box Micro-Dashboard (Screen 2)
* **Visual Style:** **Bento Grid Layout** combined with absolute **Glassmorphism**. Grid blocks hold different media aspects (memes, live portrait, interactive console).
* **Glassmorphism Specs:** * `background: rgba(255, 255, 255, 0.07);`
    * `backdrop-filter: blur(24px) saturate(180%);`
    * `border: 1px solid rgba(255, 255, 255, 0.15);`
    * `box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.2);`
* **The Runaway Logic:** The "No" button maps the bounding box of the cursor. As the vector distance approaches $< 80\text{px}$, a continuous programmatic evasion vector is calculated, translating the element (`transform: translate3d(x, y, 0)`) seamlessly across the grid boundaries while triggering subtle "shivering" CSS animations.

### C. The Euphoric Resolution (Screen 3)
* **Visual Style:** Full-bleed interactive particle field (3D floating hearts and glowing stars powered by a Canvas/WebGL engine).
* **Layout:** Confetti explosion micro-interaction on entry. Bold, celebratory typography (`THANK YOU MY LOVE, YAYYYYY`).
* **Audio Architecture:** Audio context initialization that fades in a high-fidelity, melodious lo-fi acoustic track smoothly, preventing abrupt audio clipping.

---

## 2. Component Layout & Asset Blueprint


```

```text
GEMINI.md generated successfully.


```

+-----------------------------------------------------------------------+
|  [ Screen 2: Bento Grid Layout Paradigm ]                             |
|                                                                       |
|  +-----------------------------------+  +--------------------------+  |
|  | Box A: High-Res Portrait Frame   |  | Box B: Wholesome Meme 1  |  |
|  | (Soft-zoom hover effect, golden   |  | (Floating Polaroid       |  |
|  | ratio proportions)                |  |  tilt styling)           |  |
|  +-----------------------------------+  +--------------------------+  |
|                                                                       |
|  +-----------------------------------+  +--------------------------+  |
|  | Box C: Core Question Interactivity|  | Box D: Wholesome Meme 2  |  |
|  | "Will you forgive me?"            |  | (Parallax scrolling multi|  |
|  |                                   |  |  layered image stack)    |  |
|  |  [ YES ]        [[ NO ]] (Elusive)|  |                          |  |
|  +-----------------------------------+  +--------------------------+  |
+-----------------------------------------------------------------------+

```

---

## 3. Autonomous Loop-by-Goal Prompt System

Copy and paste these systematic prompts into your AI development assistant (Gemini/Cursor/Claude) sequentially. Each prompt contains strict context windows, error-handling vectors, and precise aesthetic boundaries.

### Goal 1: Environment Setup & Core Global Design Token Infrastructure
```text
Context: You are a Lead Creative Technologist building "Project Antigravity" — a high-fidelity, high-resolution interactive web project. 
Task: Generate a production-ready modern workspace architecture utilizing vanilla HTML5, advanced modern CSS3 (using custom properties, modern nesting, and container queries), and vanilla ES6+ JavaScript. 
Aesthetic Directives:
- Design Style: Dark Mode Minimalist meets Premium Romance.
- Base background: Smooth deep velvet dark violet (#0b0813) to rich midnight indigo (#060714) linear gradient.
- Fonts: Import 'Playfair Display' (for elegant, emotional titles) and 'Inter' (for clean interface elements) from Google Fonts.
- Set up global design tokens inside a `:root` block including: neon pink accents, soft gold highlights, custom glassmorphism variables, transition timings (smooth bezier curves: `cubic-bezier(0.25, 1, 0.5, 1)`), and standard bento grid gap definitions (24px).
- Provide a unified file tree structure: `index.html`, `styles.css`, and `app.js`. Ensure the boilerplate includes full responsive meta tags and connects all assets flawlessly.

```

### Goal 2: The Cinematic Intro Screen (Screen 1 Construction)

```text
Context: We are building the first view of Project Antigravity: The Cinematic Intro.
Task: Implement the HTML and CSS structure for Screen 1 inside `index.html` and `styles.css`.
Requirements:
- Structural Layout: Full-viewport container (`width: 100vw; height: 100vh;`) with all content flawlessly centered via standard box layouts (avoiding banned layout properties; use absolute center placements or block-level margins with text alignment).
- Headline: "HI NISHI, I LOVE YOU SM" in a highly polished font size (approx 28pt to 34pt), using a text-gradient technique (shifting from pastel rose gold to luminous pearl).
- Portrait Holder: A beautifully proportioned profile frame with an explicit high-fidelity placeholder image URI. Apply a continuous `border-radius` mask, an active subtle breathing animation (`transform: scale(1.02)`), and an inner box shadow to blend it into the canvas.
- Continue Button: A gorgeous magnetic action button. Style with a semi-transparent white background (`rgba(255, 255, 255, 0.1)`), an intense backdrop blur (`12px`), a thin 1px border, and a glowing hover effect that expands outwards.
- JavaScript Transition Hook: Add data attributes or IDs so clicking this button seamlessly fades out Screen 1 and reveals Screen 2 using an elegant transition opacity fade.

```

### Goal 3: The Premium Bento Box Glassmorphism Dashboard (Screen 2 Setup)

```text
Context: Transitioning to the core interactive phase of Project Antigravity.
Task: Author the structural layout for Screen 2 using a strict Bento Grid formulation combined with advanced Glassmorphism styling sheets.
Requirements:
- Layout: Design a multi-column, multi-row layout using standard structural block wrappers. The boxes must scale dynamically based on screen real estate.
- Box Styles: Every box in the grid must feature high-end Glassmorphism (24px backdrop-filter blur, desaturated semi-transparent background fill, crisp 1px borders with varied opacity, and a soft outer drop shadow).
- Grid Inventory:
  * Box A (Double Height/Width): Main high-fidelity portrait display with an internal hover-zoom transition.
  * Box B (Standard): Curated wholesome meme image placeholder with a retro Polaroid-style off-axis tilt angle (`transform: rotate(-2deg)`).
  * Box C (Double Width): The Interaction Chamber containing the high-visibility question text: "Will you forgive me?" and two prominent UI buttons: [ Yes ] and [ No ].
  * Box D (Standard): Secondary wholesome meme frame utilizing a subtle vertical parallax shift effect.

```

### Goal 4: The Intelligent Evasion Vector Mechanics (The "No" Button Escape Logic)

```text
Context: We need to build the core programmatic trick of Project Antigravity—making the "No" button entirely unclickable by having it run away gracefully.
Task: Write the functional JavaScript inside `app.js` to handle mouse and touch proximity manipulation for the "No" button element.
Requirements:
- Interaction Physics: Attach an event listener tracking cursor coordinates relative to the bounding box of the "No" button.
- Proximity Detection: Calculate the Euclidean distance between the cursor center and the button center. If the distance falls below a threshold (e.g., 90px), calculate an escape vector pointing exactly away from the cursor's approach angle.
- Boundary Constraints: The calculated target position must keep the button strictly bounded within its parent Bento container box so it never clips off-screen or disappears completely outside the viewport.
- Motion Fluidity: Update the button's coordinates using `transform: translate3d(x, y, 0)` combined with a CSS transition set to a hyper-responsive cubic-bezier speed profile.
- Touch Support: Map touch movement events (`touchmove`) similarly so it actively dodges finger inputs on mobile environments.

```

### Goal 5: The Celebration Scene & Immersive Audio Engine (Screen 3 Integration)

```text
Context: The final phase when the user successfully clicks the "Yes" button.
Task: Construct Screen 3 ("THANK YOU MY LOVE, YAYYYYY") along with background audio integration.
Requirements:
- Interface Reveal: When [ Yes ] is clicked, trigger a full-screen transition. Screen 2 cross-fades into Screen 3 seamlessly.
- Visual FX: Initialize a custom background canvas element that spawns an active particle array of floating hearts and stardust. Implement this engine in pure JavaScript with clean rendering animation loops.
- Typography: Display the phrase "THANK YOU MY LOVE, YAYYYYY" using a luminous, warm text glow shadow.
- Audio Integration: Embed an HTML5 audio context node or audio element sourcing a beautiful, ambient melodic instrumental background track. The music must initialize dynamically upon the explicit user click of the "Yes" button (to respect modern browser auto-play media policies) and scale its volume up from 0% to 70% over a 3-second window using a programmatic fade-in loop.

```

### Goal 6: End-to-End Asset Verification, Polish, and Production Check

```text
Context: Final review phase for Project Antigravity.
Task: Perform a structural and programmatic audit of the code generated across the previous goals.
Requirements:
- Check for absolute layout stability: Ensure all element widths use appropriate box-sizing wrappers (`box-sizing: border-box`).
- Image Resiliency: Ensure all image tags contain functional, crisp, high-resolution premium placeholders and possess object-fit rules (`object-fit: cover`) to eliminate structural distortion.
- Check state machine transitions: Verify that Screen 1, Screen 2, and Screen 3 maintain explicit visibility toggling via clean, active utility classes without style collisions.
- Deliver the final, fully synthesized code blocks ready for instant compilation.

```

---

## 4. Technical Quality Standards

1. **Responsive Scaling:** Every aspect must feel locked-in on both a 4K desktop display and a handheld mobile viewport.
2. **Animation Performance:** Hardware accelerate all movements by utilizing exclusively `transform` and `opacity` mutations; avoid modifying raw position values (`top`, `left`) to prevent CPU layout thrashing.
3. **Typography Harmony:** Maintain clear vertical visual hierarchy. Titles use romantic serif typefaces; navigation and interactivity use high-readability sans-serif fonts.
"""

# Write the content to GEMINI.md

with open("GEMINI.md", "w", encoding="utf-8") as f:
f.write(md_content.strip())

print("GEMINI.md generated successfully.")

```
Your markdown architecture file and automated prompting system have been generated successfully. 

[file-tag: code-generated-file-0-1784183186549218962]

Here is a comprehensive breakdown of the project architecture, design frameworks, and step-by-step implementation guide to build this premium website for Nishi.

---

### UI/UX Design System & Layout Strategy

To make this website look incredibly expensive and visually striking, we bypass standard website structures and implement two highly modern, premium design languages: **Glassmorphism** and the **Bento Grid**.

#### 1. The Glassmorphism Framework
Every card or interface element will look like polished, frosted studio glass catching light. This is achieved using precise layering:
* **Backdrop Filter:** A high-radial blur (`blur(20px)`) that diffuses the vivid colors behind it.
* **Surface Tint:** A desaturated, ultra-thin white overlay (`rgba(255, 255, 255, 0.06)`).
* **Specular Border:** A sharp, 1px perimeter line with alternating opacities to simulate a realistic glass edge that catches light.
* **Shadow Depth:** A multi-layered ambient occlusion shadow that lifts the panels off the background canvas.

#### 2. The Bento Grid Structure (Screen 2)
Instead of a generic list or column system, Nishi's photos, memes, and the central question are arranged into a tight, visually satisfying asymmetrical layout inspired by premium hardware dashboards and modern editorial design.


```

+-------------------------------------------------------------------------+
| [ SCREEN 2: BENTO BOX LAYOUT ]                                          |
|                                                                         |
| +-------------------------------------+  +----------------------------+ |
| | BOX A: High-Resolution Portrait     |  | BOX B: Wholesome Meme #1   | |
| | (Smooth infinite micro-zoom canvas, |  | (Slightly tilted polaroid  | |
| |  luminous border glow)              |  |  border styling)           | |
| +-------------------------------------+  +----------------------------+ |
|                                                                         |
| +-------------------------------------+  +----------------------------+ |
| | BOX C: Core Interactive Panel       |  | BOX D: Wholesome Meme #2   | |
| | "Will you forgive me?"              |  | (Parallax image array      | |
| |                                     |  |  that tracks mouse drift)  | |
| |   [ YES ]       [[ NO ]] (Elusive)  |  |                            | |
| +-------------------------------------+  +----------------------------+ |
+-------------------------------------------------------------------------+

```

---

### Step-by-Step Implementation Guide

To assemble the project immediately, use the following production-ready source files.

#### 1. The Blueprint (`index.html`)
Create your main file and structure the three linear viewports.

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>For Nishi, With Love</title>
    <link rel="stylesheet" href="styles.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&family=Playfair+Display:ital,wght@0,600;1,400&display=swap" rel="stylesheet">
</head>
<body>
    <div class="aurora-bg"></div>

    <section id="screen-1" class="view-panel active">
        <div class="intro-glass-card">
            <div class="image-frame-wrapper">
                <img src="https://images.unsplash.com/photo-1534528741775-53994a69daeb?q=80&w=1000" alt="Nishi Portrait" class="hero-portrait">
            </div>
            <h1 class="romantic-title">Hi Nishi,<br><span class="gradient-text">I Love You SM</span></h1>
            <button id="btn-continue" class="premium-btn">
                <span>Continue</span>
                <div class="btn-glow"></div>
            </button>
        </div>
    </section>

    <section id="screen-2" class="view-panel">
        <div class="bento-container">
            <div class="bento-box box-portrait">
                <img src="https://images.unsplash.com/photo-1517841905240-472988babdf9?q=80&w=1000" alt="Nishi Grid Portrait" class="grid-img">
                <div class="box-overlay">My Whole World</div>
            </div>

            <div class="bento-box box-meme-1">
                <div class="polaroid-wrapper">
                    <img src="https://i.pinimg.com/736x/8a/be/1b/8abe1bca5492d5c88c7c9454cc5bbfef.jpg" alt="Cute Cat Meme" class="meme-img">
                    <p class="polaroid-caption">Me when I look at you uwu</p>
                </div>
            </div>

            <div class="bento-box box-core-question">
                <h2 class="question-text">Will you forgive me? 🥺</h2>
                <div class="action-row">
                    <button id="btn-yes" class="action-btn yes-variant">Yes</button>
                    <button id="btn-no" class="action-btn no-variant">No</button>
                </div>
            </div>

            <div class="bento-box box-meme-2">
                <div class="polaroid-wrapper">
                    <img src="https://i.pinimg.com/736x/f6/8b/6e/f68b6e6e22fa6b361bb5649938c823f9.jpg" alt="Wholesome Love Meme" class="meme-img">
                    <p class="polaroid-caption">Sending 9999+ hugs right now</p>
                </div>
            </div>
        </div>
    </section>

    <section id="screen-3" class="view-panel">
        <canvas id="hearts-canvas"></canvas>
        <div class="resolution-card">
            <div class="image-frame-wrapper floating-anim">
                <img src="https://images.unsplash.com/photo-1524504388940-b1c1722653e1?q=80&w=1000" alt="Nishi Happy Portrait" class="hero-portrait">
            </div>
            <h1 class="celebration-title">THANK YOU MY LOVE,<br><span class="yay-text">YAYYYYY! 🎉❤️</span></h1>
        </div>
    </section>

    <audio id="bg-music" loop preload="auto">
        <source src="https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3" type="audio/mpeg">
    </audio>

    <script src="app.js"></script>
</body>
</html>

```

#### 2. The High-Fidelity Styling (`styles.css`)

This file handles the modern layout styles, glass transparency effects, typography scale, and responsive transitions.

```css
*, *::before, *::after {
    box-sizing: border-box;
}

:root {
    --bg-dark: #07050f;
    --accent-pink: #ff6b8b;
    --accent-gold: #ffd166;
    --glass-bg: rgba(255, 255, 255, 0.05);
    --glass-border: rgba(255, 255, 255, 0.12);
    --glass-shadow: 0 20px 50px rgba(0, 0, 0, 0.3);
    --font-serif: 'Playfair Display', serif;
    --font-sans: 'Inter', sans-serif;
    --transition-smooth: all 0.6s cubic-bezier(0.16, 1, 0.3, 1);
}

body {
    margin: 0;
    padding: 0;
    background-color: var(--bg-dark);
    font-family: var(--font-sans);
    color: #ffffff;
    overflow: hidden;
    height: 100vh;
    width: 100vw;
}

/* Ambient Animated Aurora Mesh Background */
.aurora-bg {
    position: fixed;
    top: -50%;
    left: -50%;
    right: -50%;
    bottom: -50%;
    width: 200%;
    height: 200%;
    background: radial-gradient(circle at 30% 20%, #2c1654 0%, transparent 40%),
                radial-gradient(circle at 70% 60%, #591a38 0%, transparent 45%),
                radial-gradient(circle at 50% 40%, #11143a 0%, transparent 50%);
    z-index: -1;
    filter: blur(80px);
    animation: auroraDrift 25s ease infinite alternate;
}

@keyframes auroraDrift {
    0% { transform: rotate(0deg) scale(1); }
    100% { transform: rotate(8deg) scale(1.1); }
}

/* Structural Panels Management */
.view-panel {
    position: absolute;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    opacity: 0;
    pointer-events: none;
    transform: scale(0.96);
    transition: var(--transition-smooth);
    z-index: 1;
}

.view-panel.active {
    opacity: 1;
    pointer-events: auto;
    transform: scale(1);
    z-index: 2;
}

/* Glassmorphism Card Core Blueprint */
.intro-glass-card, .resolution-card {
    background: var(--glass-bg);
    backdrop-filter: blur(25px) saturate(160%);
    border: 1px solid var(--glass-border);
    border-radius: 32px;
    padding: 40px;
    text-align: center;
    box-shadow: var(--glass-shadow);
    max-width: 440px;
    width: 90%;
}

.image-frame-wrapper {
    width: 160px;
    height: 160px;
    margin: 0 auto 24px auto;
    border-radius: 40px;
    overflow: hidden;
    border: 2px solid rgba(255, 255, 255, 0.2);
    box-shadow: 0 10px 30px rgba(0,0,0,0.5);
}

.hero-portrait {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: var(--transition-smooth);
}

.image-frame-wrapper:hover .hero-portrait {
    transform: scale(1.08);
}

.romantic-title {
    font-family: var(--font-serif);
    font-size: 2.2rem;
    font-weight: 600;
    line-height: 1.3;
    margin-bottom: 32px;
}

.gradient-text {
    background: linear-gradient(45deg, #ff758c, #ff7eb3, #ffd166);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
}

/* Premium Motion Buttons */
.premium-btn {
    position: relative;
    background: rgba(255, 255, 255, 0.08);
    border: 1px solid rgba(255, 255, 255, 0.2);
    color: white;
    padding: 14px 36px;
    font-size: 1rem;
    font-weight: 600;
    border-radius: 16px;
    cursor: pointer;
    overflow: hidden;
    transition: all 0.3s ease;
}

.premium-btn:hover {
    background: white;
    color: var(--bg-dark);
    box-shadow: 0 0 30px rgba(255, 255, 255, 0.4);
    transform: translateY(-2px);
}

/* Premium Bento Grid Mechanics */
.bento-container {
    display: block; /* Native blocks for print safety & precise placement */
    width: 92%;
    max-width: 960px;
    margin: auto;
}

.bento-box {
    background: var(--glass-bg);
    backdrop-filter: blur(20px);
    border: 1px solid var(--glass-border);
    border-radius: 24px;
    padding: 24px;
    margin-bottom: 20px;
    float: left;
    box-shadow: var(--glass-shadow);
    position: relative;
    overflow: hidden;
}

/* Complex Asymmetric Sizing Logic */
.box-portrait { width: 58%; height: 320px; margin-right: 2%; }
.box-meme-1   { width: 40%; height: 320px; }
.box-core-question { width: 48%; height: 240px; margin-right: 2%; clear: left; }
.box-meme-2   { width: 50%; height: 240px; }

.grid-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    border-radius: 14px;
}

.box-overlay {
    position: absolute;
    bottom: 20px;
    left: 20px;
    background: rgba(0, 0, 0, 0.6);
    backdrop-filter: blur(8px);
    padding: 6px 14px;
    border-radius: 20px;
    font-size: 0.85rem;
    font-weight: 500;
}

/* Polaroid Sub-Styling Architecture */
.polaroid-wrapper {
    background: white;
    padding: 12px 12px 20px 12px;
    box-shadow: 0 15px 35px rgba(0,0,0,0.4);
    transform: rotate(-1.5deg);
    height: 100%;
    display: flex;
    flex-direction: column;
    transition: var(--transition-smooth);
}

.box-meme-2 .polaroid-wrapper {
    transform: rotate(2deg);
}

.polaroid-wrapper:hover {
    transform: rotate(0deg) scale(1.03);
}

.meme-img {
    width: 100%;
    height: 78%;
    object-fit: cover;
}

.polaroid-caption {
    color: #222;
    font-family: var(--font-serif);
    font-size: 0.9rem;
    text-align: center;
    margin: 12px 0 0 0;
    font-weight: 600;
}

/* Question Console Interactivity styles */
.box-core-question {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
}

.question-text {
    font-family: var(--font-serif);
    font-size: 1.6rem;
    margin: 0 0 24px 0;
}

.action-row {
    position: relative;
    width: 100%;
    height: 60px;
}

.action-btn {
    position: absolute;
    padding: 12px 36px;
    font-size: 1rem;
    font-weight: 600;
    border-radius: 14px;
    cursor: pointer;
    border: none;
    transition: background 0.3s, transform 0.1s linear;
}

.yes-variant {
    background: linear-gradient(135deg, #ff758c, #ff7eb3);
    color: white;
    left: 15%;
    box-shadow: 0 10px 20px rgba(255, 117, 140, 0.3);
}

.yes-variant:hover {
    transform: scale(1.05);
    box-shadow: 0 15px 25px rgba(255, 117, 140, 0.5);
}

.no-variant {
    background: rgba(255, 255, 255, 0.15);
    color: white;
    right: 15%;
    border: 1px solid rgba(255, 255, 255, 0.2);
    will-change: transform, left, top;
}

/* Screen 3 Custom Polish */
#hearts-canvas {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    z-index: 1;
}

.celebration-title {
    font-family: var(--font-serif);
    font-size: 2.4rem;
    line-height: 1.4;
}

.yay-text {
    background: linear-gradient(45deg, #ffd166, #ff6b8b);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    font-size: 3rem;
    display: inline-block;
    margin-top: 10px;
}

.floating-anim {
    animation: smoothFloat 4s ease-in-out infinite alternate;
}

@keyframes smoothFloat {
    0% { transform: translateY(0); }
    100% { transform: translateY(-12px); }
}

```

#### 3. The Interaction Engine (`app.js`)

This script manages screen transitions, the advanced vector movement calculation that causes the "No" button to elegantly flee, and the browser-compliant background music initialization.

```javascript
document.addEventListener("DOMContentLoaded", () => {
    const screen1 = document.getElementById("screen-1");
    const screen2 = document.getElementById("screen-2");
    const screen3 = document.getElementById("screen-3");
    
    const btnContinue = document.getElementById("btn-continue");
    const btnYes = document.getElementById("btn-yes");
    const btnNo = document.getElementById("btn-no");
    const bgMusic = document.getElementById("bg-music");

    // Unified Cross-Fade Screen Architecture
    function navigateTo(fromScreen, toScreen) {
        fromScreen.classList.remove("active");
        setTimeout(() => {
            toScreen.classList.add("active");
        }, 300);
    }

    // Step 1 to Step 2 Transition
    btnContinue.addEventListener("click", () => {
        navigateTo(screen1, screen2);
        // Start background music smoothly after explicit user event
        bgMusic.volume = 0;
        bgMusic.play().then(() => {
            fadeAudioIn(bgMusic, 0.6, 2500);
        }).catch(err => console.log("Audio play blocked until next interaction"));
    });

    // Step 2 to Step 3 Transition (Acceptance)
    btnYes.addEventListener("click", () => {
        navigateTo(screen2, screen3);
        initHeartsParticleSystem();
    });

    // Runaway "No" Button Proximity Vector Evasion Logic
    const container = document.querySelector(".box-core-question");

    function runAway(event) {
        // Track input location based on mouse or touch positions
        const clientX = event.touches ? event.touches[0].clientX : event.clientX;
        const clientY = event.touches ? event.touches[0].clientY : event.clientY;

        const btnRect = btnNo.getBoundingClientRect();
        const containerRect = container.getBoundingClientRect();

        // Calculate center point coordinates of the target button
        const btnCenterX = btnRect.left + btnRect.width / 2;
        const btnCenterY = btnRect.top + btnRect.height / 2;

        // Vector calculations mapping proximity distance
        const deltaX = clientX - btnCenterX;
        const deltaY = clientY - btnCenterY;
        const distance = Math.hypot(deltaX, deltaY);

        // Proximity detection bubble threshold (Trigger radius = 85px)
        if (distance < 85) {
            // Determine escape vectors
            const angle = Math.atan2(deltaY, deltaX);
            const escapeDist = 100; // Multiplier of evasion burst jump

            // Project new coordinates moving away from approach path
            let targetX = btnNo.offsetLeft - Math.cos(angle) * escapeDist;
            let targetY = btnNo.offsetTop - Math.sin(angle) * escapeDist;

            // Restrict movement targets within the boundaries of the Bento Box container
            const padding = 20;
            const minX = padding;
            const maxX = containerRect.width - btnRect.width - padding;
            const minY = padding;
            const maxY = containerRect.height - btnRect.height - padding;

            // Bound checking adjustments
            if (targetX < minX) targetX = maxX - Math.random() * 30;
            if (targetX > maxX) targetX = minX + Math.random() * 30;
            if (targetY < minY) targetY = maxY - Math.random() * 30;
            if (targetY > maxY) targetY = minY + Math.random() * 30;

            // Apply calculated hardware accelerated layout translations
            btnNo.style.left = `${targetX}px`;
            btnNo.style.top = `${targetY}px`;
        }
    }

    // Bind interaction triggers across standard and touch devices
    container.addEventListener("mousemove", runAway);
    container.addEventListener("touchmove", runAway, { passive: true });

    // Smooth Audio Fade Routine
    function fadeAudioIn(audioNode, targetVolume, duration) {
        let currentVol = 0;
        const intervalTime = 50;
        const step = targetVolume / (duration / intervalTime);
        
        const fadeInterval = setInterval(() => {
            currentVol += step;
            if (currentVol >= targetVolume) {
                audioNode.volume = targetVolume;
                clearInterval(fadeInterval);
            } else {
                audioNode.volume = currentVol;
            }
        }, intervalTime);
    }

    // Canvas Engine: Ambient Rising Celebration Particles
    function initHeartsParticleSystem() {
        const canvas = document.getElementById("hearts-canvas");
        const ctx = canvas.getContext("2d");
        
        function resize() {
            canvas.width = window.innerWidth;
            canvas.height = window.innerHeight;
        }
        window.addEventListener("resize", resize);
        resize();

        const particles = [];
        const heartCount = 65;

        class HeartParticle {
            constructor() {
                this.reset();
                this.y = Math.random() * canvas.height; // Spread initially
            }
            reset() {
                this.x = Math.random() * canvas.width;
                this.y = canvas.height + 20;
                this.size = Math.random() * 14 + 8;
                this.speedY = -(Math.random() * 2 + 1);
                this.speedX = Math.sin(Math.random() * 4) * 0.6;
                this.opacity = Math.random() * 0.5 + 0.4;
            }
            update() {
                this.y += this.speedY;
                this.x += this.speedX;
                if (this.y < -20) this.reset();
            }
            draw() {
                ctx.save();
                ctx.globalAlpha = this.opacity;
                ctx.fillStyle = "#ff6b8b";
                ctx.beginPath();
                // Draw heart shape via bezier vector points
                const topY = this.y - this.size / 2;
                ctx.moveTo(this.x, topY + this.size / 4);
                ctx.bezierCurveTo(this.x, topY, this.x - this.size / 2, topY, this.x - this.size / 2, topY + this.size / 4);
                ctx.bezierCurveTo(this.x - this.size / 2, topY + this.size / 2, this.x, topY + this.size * 0.75, this.x, this.y);
                ctx.bezierCurveTo(this.x, topY + this.size * 0.75, this.x + this.size / 2, topY + this.size / 2, this.x + this.size / 2, topY + this.size / 4);
                ctx.bezierCurveTo(this.x + this.size / 2, topY, this.x, topY, this.x, topY + this.size / 4);
                ctx.closePath();
                ctx.fill();
                ctx.restore();
            }
        }

        for (let i = 0; i < heartCount; i++) {
            particles.push(new HeartParticle());
        }

        function animate() {
            ctx.clearRect(0, 0, canvas.width, canvas.height);
            particles.forEach(p => {
                p.update();
                p.draw();
            });
            requestAnimationFrame(animate);
        }
        animate();
    }
});

```

---

### Tips for Customizing the Final Look:

1. **Swap the Placeholders:** Replace the `src="..."` URLs inside `index.html` with actual high-resolution photos of Nishi or links to your favorite pictures of her hosted online.
2. **Choose a Unique Song:** Replace the `.mp3` source under the audio node with an open link to a custom music track (e.g., a lo-fi guitar or soft piano melody). Use these pics of her wherever they are needed on random sort.
